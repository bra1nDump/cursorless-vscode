a = ^{ return getCSE(); };

a = @{
    @2: foo
};

@protocol SDWebImage <Object>
+ (NSArray*)extendedAttributeNamesAtPath:(NSString *)path traverseLink:(BOOL)follow ;
@end

@interface SDWebImage

+ (NSArray*)extendedAttributeNamesAtPath:(NSString *)path traverseLink:(BOOL)follow ;

@end

enum ShapeType {
    kCircle,
    kRectangle,
    kOblateSpheroid
} ;

@implementation SDWebImage

+ (NSArray*)extendedAttributeNamesAtPath:(NSString *)path traverseLink:(BOOL)follow {
    for (NSNumber * _Nonnull foo in foos) {
        foo = [subview method];
    }
    // pp
    if (YES) {
        NSNumber f = follow + 1 + @"pp";
    }
    int p;
    return [[NSArray alloc] initWith: @[@1, path]];
}


@end
